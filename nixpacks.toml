[phases.install]
cmds = [
  "npm install -g corepack@0.24.1 && corepack enable",
  "pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = ["pnpm turbo build --filter=api"]

[start]
cmd = "node apps/api/dist/main"
